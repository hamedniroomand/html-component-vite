<div class="flex flex-col items-center">
    <img src="{{logo}}" alt="{{name}} logo" class="h-16 mb-4">
    <h3 class="text-lg font-semibold text-gray-900 dark:text-white">{{name}}</h3>
    <p class="mt-2 text-gray-600 dark:text-gray-300 text-center text-sm">{{description}}</p>
</div>